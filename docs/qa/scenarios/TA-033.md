---
id: TA-033
area: TA
title: Recover needs_attention run
persona: Operator
journey:
expected: POST recover terminalizes + queues continuation (201).
entry_points: `POST /api/runs/:id/recover`; CLI `task recover`
qa_status: pass
bug_ids:
fix_status:
retest_status:
fix_commits:
evidence: `tasks.go:1471-1507`; route `httpapi:336`; CLI `task.go:1670`; qa/evidence/batch-007-task-run-lifecycle/ta-033.json; qa/evidence/batch-007-task-run-lifecycle/ta024-ta034-summary.json
last_report:
overlaps:
---

story: As a user I recover a needs_attention run by terminalizing it and queuing a fresh child.

errors: None observed during isolated HTTP and UDS-backed CLI QA.

HTTP and UDS-backed CLI recover paths terminalized needs_attention runs and queued continuations. Browser/web affordance coverage remains blocked where applicable.

src: docs/qa/_seeds/feature-stories/02_analysis_tasks-automation.md

inventory: Needs QA
