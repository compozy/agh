---
id: ET-018
area: ET
title: Install extension (marketplace slug + trust)
persona: Operator/Agent
journey:
expected: `POST /api/extensions {slug,source,version,asset}` returns 201; blocked trust → error; web exposes `allow_unverified` toggle.
entry_points: `POST /api/extensions`, web install button, `agh__extensions_install`
qa_status: blocked-verify
bug_ids: BUG-0008
fix_status:
retest_status:
fix_commits:
evidence: `extensions.go:93-128`; `hooks-extensions.tsx:758-787,871-917`; qa/evidence/batch-017-extensions-local-marketplace/et015-et023.json; qa/evidence/batch-017-extensions-local-marketplace/et015-et023-summary.json; qa/issues/BUG-008.md
last_report:
overlaps:
---

story: As an operator I install a marketplace extension by slug with trust/allow_unverified gating.

errors: Marketplace slug install returned 503 because extensions.marketplace registry is not configured in the isolated lab. No product failure after BUG-008; actual install and web trust badge require configured registry.

migrated-status: qa=Blocked | error=Configuration blocker | retest=Blocked

Verify allow_unverified path and trust badge after a real extension marketplace is configured.

src: docs/qa/_seeds/feature-stories/03_analysis_extensibility-tools.md

inventory: Potential UX/logistics risk
