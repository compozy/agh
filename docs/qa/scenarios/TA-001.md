---
id: TA-001
area: TA
title: Create task
persona: Operator/Agent
journey:
expected: POST creates a task (201) with title, scope (global/workspace), priority, max_attempts, approval_policy, auto_enqueue_on_ready, and optional draft + owner; the staging contract shell may still deserialize `network_channel`, but every non-empty raw value is rejected, while the Web modal never exposes or sends that raw execution field and offers templates one_shot/recurring/epic/remote_peer/human_in_loop/blank. Invalid scope binding → 4xx.
entry_points: Web `/tasks/new` modal; `POST /api/tasks`; `POST (uds) /tasks`; CLI `task create`
qa_status: untested
bug_ids:
fix_status:
retest_status:
fix_commits:
evidence: `tasks.go:188-223,1733-1766`; routes `httpapi:269`,`uds:282`; `web/.../tasks.new.tsx:11-19`; `task-templates.ts:55-131`; qa/evidence/batch-005-tasks-crud/ta001-*; qa/evidence/batch-005-tasks-crud/ta001-ta014-summary.json; qa/evidence/batch-005-tasks-crud/ta001-ta014-retest-summary.json
last_report:
overlaps:
---

story: As a user I create a task (optionally from a template) so work can be queued.

errors: HTTP create workspace task returned 201 for task-00b150468e8e99a2; workspace-scope create without workspace returned client error; UDS-backed CLI create returned task-8af6f174bc83d375.

Verify template prefills; workspace scope requires workspace binding. Web-specific UI rendering for this task story remains browser-blocked where applicable; HTTP/UDS behavior passed.

src: docs/qa/_seeds/feature-stories/02_analysis_tasks-automation.md

inventory: Needs QA
