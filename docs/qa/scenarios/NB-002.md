---
id: NB-002
area: NB
title: Network settings config
persona: Operator
journey:
expected: `settings/network` edits `enabled`, `port`, `default_channel`, `greet_interval`, `max_payload`, `max_queue_depth`, `max_replay_age`; topology changes (port/enabled) raise a restart banner; runtime metrics shown read-only.
entry_points: web `settings/network`; `GET`/`PATCH /api/settings/network`
qa_status: pass
bug_ids:
fix_status:
retest_status: pass
fix_commits:
evidence: qa/evidence/batch-023-network-runtime-channels/nb001-nb023-final-summary.json; qa/evidence/batch-023-network-runtime-channels/nb001-nb023-before-bug012.json; qa/evidence/batch-023-network-runtime-channels/bug012-retest-direct-send.json
last_report:
overlaps: MS-037
---

story: As an operator I want to configure the embedded network (port, default channel, delivery limits) so it fits my deployment.

migrated-status: qa=Passed with corrected harness expectation | retest=Passed after harness correction

overlap: Network-domain settings behavior; overlaps generic settings section MS-037.

GET /api/settings/network returned config/runtime. PATCH returns a restart-required apply record for network config by design; retest restored the original config and verified it with GET.

src: docs/qa/_seeds/feature-stories/04_analysis_network-bridges.md

inventory: QA complete
