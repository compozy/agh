---
id: TA-069
area: TA
title: Loop config and annotations API
persona: Operator/Agent
journey:
expected: GET/PUT /config and GET/PUT /annotations persist workspace-scoped Loop config overrides and editor positions without leaking same-name loops across workspaces.
entry_points: HTTP/UDS Loop config and annotations routes
qa_status: untested
bug_ids:
fix_status:
retest_status:
fix_commits:
evidence:
last_report:
overlaps:
---

story: As an operator or editor I manage Loop runtime defaults and diagram annotations per workspace.

Added by Task 14 QA tracker impact; flag only, not retested.

src: .compozy/tasks/loops/task_14.md
