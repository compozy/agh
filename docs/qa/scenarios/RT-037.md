---
id: RT-037
area: RT
title: Sandbox profile management
persona: Operator/Agent
journey:
expected: `GET/PUT/DELETE /api/settings/sandboxes[/:name]` (HTTP PUT/DELETE privileged); backends `local`/`daytona`; PUT is full replace; builtin profiles cannot be deleted (only overridden); usage count tracks referencing workspaces.
entry_points: HTTP+UDS settings; web `/_app/sandbox`
qa_status: pass
bug_ids: BUG-0004
fix_status: fixed
retest_status: pass
fix_commits:
evidence: `routes.go:482-485`; `sandbox.tsx:44-609`; qa/evidence/batch-004-context-settings/rt037-*; qa/issues/BUG-004.md
last_report:
overlaps: MS-030
---

story: As an operator I want to create/edit/delete sandbox profiles referenceable by workspaces

errors: BUG-004: replacing an existing sandbox profile after it had nested env/network subtables returned HTTP 409 unsupported TOML mutation instead of full-replacing the profile. After the fix, PUT full profile, PUT minimal replacement, GET without stale nested fields, UDS-backed config list, usage_count=1 while a workspace referenced a temporary profile, restore-to-local, and delete all passed.

migrated-status: qa=Passed after fix | error=Resolved BUG-004 | retest=Passed

overlap: Canonical owner for sandbox profile behavior; overlaps MS-030.

The settings collection exposes configured sandbox overlays; builtin `local` was not returned as a deletable collection item in this lab, so builtin-delete UI disabling remains browser-blocked. Full replace and usage-count API behavior are covered.

src: docs/qa/_seeds/feature-stories/01_analysis_runtime-sessions.md

inventory: Needs QA
