---
id: TA-055
area: TA
title: Job schedule modes
persona: Operator
journey:
expected: Cron expression, Go-duration interval, or at-time drives next-run; catch-up policy is one of `skip`, `coalesce`, or `replay` and handles missed intervals.
entry_points: Web job-form cron-builder/interval/schedule-at; job `schedule` field
qa_status: untested
bug_ids:
fix_status:
retest_status:
fix_commits:
evidence: `automation/types.go:34-74`; `schedule.go:505-516,888-909`; `web/.../job-form/cron-builder.tsx`; qa/evidence/batch-011-automation-crud-webhooks/ta-055.json; qa/evidence/batch-011-automation-crud-webhooks/ta052-ta060-summary.json
last_report:
overlaps:
---

story: As a user I schedule a job by cron, interval (`every`), or at-time with catch-up.

errors: None observed during isolated HTTP and UDS-backed CLI/API QA.

Cron, every, and at schedule creation passed; scheduler state exposed next-run/catch-up data; invalid HTTP cron and CLI `every:0s` were rejected.

AGH-66 reset: catch-up contract changed from skip-only to `skip`/`coalesce`/`replay`; flag only, not retested.

src: docs/qa/_seeds/feature-stories/02_analysis_tasks-automation.md

inventory: Needs QA
