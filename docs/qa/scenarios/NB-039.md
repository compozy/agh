---
id: NB-039
area: NB
title: Test bridge delivery (dry run)
persona: Operator/Agent
journey:
expected: `POST /api/bridges/:id/test-delivery` resolves the typed delivery target without a live adapter; returns `status=resolved`, message echo, and delivery target.
entry_points: `POST .../test-delivery`; CLI `bridge test-delivery`; web test-delivery dialog
qa_status: untested
bug_ids: BUG-0014
fix_status: fixed
retest_status: pass
fix_commits:
evidence: qa/evidence/batch-024-bridges-notifications/nb024-nb046-final-summary.json; qa/evidence/batch-024-bridges-notifications/nb024-nb046-rerun.json; qa/issues/BUG-014.md; qa/evidence/batch-024-bridges-notifications/nb034-nb035-target-refresh-retest-3.json
last_report:
overlaps:
---

story: As an operator I want to verify a bridge can resolve an outbound target without sending.

errors: BUG-014: stale bridge target directory did not refresh on list/resolve before returning user-facing target data.

migrated-status: qa=Passed API/CLI where available; web blocked | error=BUG-014 fixed | retest=Passed after on-demand target refresh retest.

Web test-delivery dialog remains browser-blocked; CLI/API dry run passed with refreshed targets.

Hermes bridge Task 05 changed the Web contract: the dry-run action is labeled `Check target
(dry run)`, while the distinct `Send test message` action performs real provider delivery. Reset to
`untested` for the next QA cycle; historical API/CLI evidence remains above.

src: docs/qa/_seeds/feature-stories/04_analysis_network-bridges.md

inventory: QA complete
