---
id: NB-001
area: NB
title: Network runtime status
persona: Operator/Agent
journey:
expected: `GET /api/network/status` returns availability, truthful `disabled|ready|active` state, local Live participant and channel counts, sent/received/rejected/delivered counters, conversation/work/direct counters, declared channels, and per-kind metrics. It exposes no listener, broker-worker, remote-peer, or queue state. Web settings and `agh network status` show the same projection.
entry_points: `GET /api/network/status`; CLI `network status`; web `settings/network` topbar
qa_status: untested
bug_ids:
fix_status:
retest_status: pass
fix_commits:
evidence: qa/evidence/batch-023-network-runtime-channels/nb001-nb023-final-summary.json; qa/evidence/batch-023-network-runtime-channels/nb001-nb023-before-bug012.json; qa/evidence/batch-023-network-runtime-channels/bug012-retest-direct-send.json
last_report:
overlaps:
---

story: As an operator I want to distinguish Network availability from current Live participation.

migrated-status: retest=Passed

Previous evidence predates the in-process Local/Live status contract. Retest HTTP, UDS, CLI, and Web
parity for disabled, ready, and active states.

src: docs/qa/_seeds/feature-stories/04_analysis_network-bridges.md

inventory: QA complete
