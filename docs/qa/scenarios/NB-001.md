---
id: NB-001
area: NB
title: Network runtime status
persona: Operator/Agent
journey:
expected: `GET /api/network/status` returns `enabled`, `status`, listener host/port, local/remote peer counts, channels, queued messages/sessions, delivery workers, sent/received/rejected/delivered counters, open threads/directs/work, conversation/work/direct transition counters, last disconnect, and per-kind metrics. Web settings shows a status line; CLI `agh network status` prints same.
entry_points: `GET /api/network/status`; CLI `network status`; web `settings/network` topbar
qa_status: pass
bug_ids:
fix_status:
retest_status: pass
fix_commits:
evidence: qa/evidence/batch-023-network-runtime-channels/nb001-nb023-final-summary.json; qa/evidence/batch-023-network-runtime-channels/nb001-nb023-before-bug012.json; qa/evidence/batch-023-network-runtime-channels/bug012-retest-direct-send.json
last_report:
overlaps:
---

story: As an operator I want to see the network runtime status so I know if the embedded network is healthy.

migrated-status: retest=Passed

HTTP /api/network/status and UDS-backed CLI network status returned enabled/running status before and after sends; status counters remained schema-compatible.

src: docs/qa/_seeds/feature-stories/04_analysis_network-bridges.md

inventory: QA complete
