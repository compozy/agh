---
id: RT-002
area: RT
title: Doctor diagnostics
persona: Operator/Agent
journey:
expected: `GET /api/doctor[?only=&exclude=&quiet=]` → 200 with `status(ok/warn/error)`, `summary.counts_by_severity`, and `items[]` (daemon, config, automation, bridge, network, skills, log-tail, task, providers, mcp) each with severity + suggested command.
entry_points: HTTP+UDS `GET /api/doctor`
qa_status: pass
bug_ids:
fix_status:
retest_status:
fix_commits:
evidence: `status.go:47-171`; `routes.go:50-53`
last_report:
overlaps:
---

story: As an operator I want runtime diagnostics so I can triage daemon/provider/config/network/skill/task issues

errors: Evidence: qa/evidence/batch-001-runtime-entrypoints/rt002-http-doctor*.json. Doctor returned 200 with status=error, summary counts, and 34 diagnostic items; only=daemon returned the daemon-side categories; only=provider returned 26 provider diagnostics; exclude=provider returned zero provider items and status=ok. The supported category name is singular provider; exclude=providers was intentionally not treated as an alias.

Test `only`/`exclude` filtering; verify provider items appear only when included.

src: docs/qa/_seeds/feature-stories/01_analysis_runtime-sessions.md

inventory: Needs QA
