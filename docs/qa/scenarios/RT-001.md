---
id: RT-001
area: RT
title: Daemon status
persona: Operator/Agent
journey:
expected: `GET /api/status[?workspace_id=]` → 200 with `daemon{status:running,pid,started_at,http_port,active_sessions,total_sessions,version}`, `sessions{active,total,by_status,by_badge}`, plus health/memory/automation/tasks/bridges/providers/mcp/skills/config/log_tail sections. Errors → 500 with message.
entry_points: HTTP+UDS `GET /api/status`; web home `/_app/`
qa_status: pass
bug_ids: BUG-0003
fix_status: fixed
retest_status: pass
fix_commits:
evidence: `status.go:34-123`; `httpapi/routes.go:50-53`; `routes/_app/index.tsx`; qa/evidence/batch-003-sessions/bug003-status-after-failed-start.json; qa/issues/BUG-003.md
last_report:
overlaps:
---

story: As an operator I want a single status payload so I can see daemon health, sessions, providers, memory, automation, network, skills, config at a glance

errors: BUG-003: failed pre-activation sessions were omitted from health.failures until daemon restart; initial status contract evidence remains valid

migrated-status: qa=Passed after BUG-003 retest | error=BUG-003 resolved | retest=Retested: status health.failures updates immediately after fresh provider-auth failed start without daemon restart

Assert status code AND body sections; verify provider messages are redacted. BUG-003 retest confirms failed-start health projection updates immediately.

src: docs/qa/_seeds/feature-stories/01_analysis_runtime-sessions.md
