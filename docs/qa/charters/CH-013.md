# CH-013: An accessibility-reliant operator configures a Loop by keyboard — is the sheet a usable focus trap?

```yaml
charter:
  id: CH-013
  mission: "As a keyboard + screen-reader operator, open and cancel the Configure sheet without a mouse — and prove the sheet traps and releases focus, every control is labelled, and cancelling writes nothing."
  mode: charter-with-tour
  persona:
    name: Sol
    device: desktop
    network: wifi-fast
    locale: en-US
  journey: J-05
  scenarios: [LP-020]
  tour: Back-Button Tour
  time_box_minutes: 60
  guidance:
    must_try:
      - "Open the Configure sheet by keyboard: focus must enter the sheet and be trapped; the dimmed backdrop must not be a keyboard dead-end."
      - "Tab every group (checks, human gate, re-attempt strategy, limits): every switch and field must carry a label announced by the screen reader."
      - "Edit a field, then close/cancel by Escape: focus must return to the invoking control and NOTHING must be written (reopen shows the last saved config)."
      - "Confirm the conditional command field's enable/disable state is announced, not just visually greyed."
    must_avoid:
      - "Non-a11y configure behavior (CH-006 owns that); the run-detail a11y sweep (CH-011 owns J-03)."
  design_reference: "loop-configure.html (LOOPS-DESIGN-SPEC §4.7 sheet)"
  truthful_ui_check: "the sheet is a focus trap that releases on close; every control labelled; cancel writes nothing (no ghost state); state changes announced not color-only."
```

<!-- Accessibility charter for J-05 (split out of CH-011 so each a11y charter walks a single journey).
Tour note: the canonical catalog has no accessibility tour, so Back-Button is used as the focus-return/escape lens; the broader control-labelling + state-announcement a11y sweep rides `must_try`. Propose an "Accessibility Tour" catalog candidate in the debrief. -->

## Debrief — <YYYY-MM-DD> (<report path>)

- **Ran:** <started> → <ended> (box respected: yes/no)
- **Findings:**
- **Bugs filed/updated:**
- **Scenarios settled:**
- **Paper cuts:**
- **Surprises:**
- **Suggested next charter:**
