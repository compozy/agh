# CH-001: A first-time adopter runs a default dev-cycle Loop — is arrive-and-use really no harder than Compozy?

```yaml
charter:
  id: CH-001
  mission: "As a first-time adopter, run software-delivery from the catalog with a couple of inputs and reach a run — and find every place the arrive-and-use path is one step harder than Compozy."
  mode: charter-with-tour
  persona:
    name: Lea
    device: laptop
    network: wifi-fast
    locale: en-US
  journey: J-01
  scenarios: [LP-001, LP-002, ET-052]
  tour: Feature Tour
  time_box_minutes: 60
  guidance:
    must_try:
      - "Land on /loops cold: is the primary action (Run) obvious without reading docs? Time-to-first-run."
      - "Filter by kind + category; confirm the empty Custom group is hidden, not an empty shelf."
      - "Open the run form: is every declared input's type + requiredness legible? Is Run correctly gated until slug is set?"
      - "Confirm NO Cost cap (USD) input and that override defaults read the canonical values (cap 50, unbounded as ∞) — not the design HTML's 10/2.0M."
      - "Disable the dev-cycle extension (`agh extensions disable dev-cycle`): the catalog must go truthfully empty (both Loops, the default agents, the ext__dev_cycle__* tools, and the coderabbit_pr_review watch source all leave together); re-enable and confirm everything returns (ET-052)."
    must_avoid:
      - "Configure/fork surfaces (CH-006/CH-007) and the editor canvas."
      - "Deep run-monitor assertions (that is CH-012)."
  design_reference: "loops-catalog.html (§4.1) + loop-run-form.html (§4.3)"
  truthful_ui_check: "No Cost cap input; canonical stop-limit defaults render; the catalog last-outcome pill is truthful (no coercion); a disabled dev-cycle leaves no ghost catalog entries."
```

<!-- The charter is durable: re-run it in later cycles and append a fresh debrief per run. -->

## Debrief — <YYYY-MM-DD> (<report path>)

- **Ran:** <started> → <ended> (box respected: yes/no)
- **Findings:**
- **Bugs filed/updated:**
- **Scenarios settled:**
- **Paper cuts:**
- **Surprises:**
- **Suggested next charter:**
