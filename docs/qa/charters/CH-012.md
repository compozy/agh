# CH-012: A power operator ships daily — do the run monitor and meters stay truthful under a full run?

```yaml
charter:
  id: CH-012
  mission: "As a delivery operator, run software-delivery to a terminal outcome and live on the run page — and prove the contract header, five meters, and terminal banner never lie about state, cost, or outcome."
  mode: charter-with-tour
  persona:
    name: Bruno
    device: desktop
    network: wifi-fast
    locale: en-US
  journey: J-01
  scenarios: [LP-003, LP-004, LP-005]
  tour: Feature Tour
  time_box_minutes: 60
  guidance:
    must_try:
      - "Run to a genuine done and confirm the terminal banner is a real verified done — force an exhausted/stalled run and confirm it is NOT coerced to done."
      - "Watch the 5 meters (Attempts/Tokens/Wall/Cost/Breadth): warn-tint only near a ceiling; the Cost meter is display-only with NO cap control."
      - "Set per-run overrides and confirm the clamp holds and the 'overrides set' badge flips; confirm canonical defaults (cap 50, ∞) render, not the design HTML's 10/2.0M."
      - "Close the tab mid-run and re-find the run via global Runs — meters and timeline must be intact (no optimistic-UI lie on reload)."
      - "Confirm the timeline shows collapsible generations + node spine with carried-forward read-only tags and gate verdict + routing."
    must_avoid:
      - "First-run onboarding friction (CH-001 owns that) and pause/stop (CH-003)."
  design_reference: "run-detail.html (§4.4 contract header + meters + timeline) + runs.html (§4.5)"
  truthful_ui_check: "no coerced done; Cost display-only with no cap control; overrides clamp; canonical defaults; reload shows true server-side state."
```

## Debrief — <YYYY-MM-DD> (<report path>)

- **Ran:** <started> → <ended> (box respected: yes/no)
- **Findings:**
- **Bugs filed/updated:**
- **Scenarios settled:**
- **Paper cuts:**
- **Surprises:**
- **Suggested next charter:**
