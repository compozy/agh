# CH-009: An evaluator attaches a schedule to a Loop — is the start[] allowlist enforced?

```yaml
charter:
  id: CH-009
  mission: "As an evaluator, read a Loop's Start bindings and attach a schedule/trigger pointed at it — and prove a binding outside the declared start[] is rejected, not silently dropped, and a valid one fires a real loop_run."
  mode: charter-with-tour
  persona:
    name: Marina
    device: desktop
    network: wifi-fast
    locale: en-US
  journey: J-09
  scenarios: [LP-033, LP-034, LP-035]
  tour: Back-Button Tour
  time_box_minutes: 60
  guidance:
    must_try:
      - "Read the Start bindings panel: declared start[] kinds as read-only chips + each attached automation with kind, enabled dot, and meta (cron+next fire / endpoint / event)."
      - "Add a schedule via 'Add schedule': confirm the create sheet opens pre-targeted, the Target step is 'Run loop' with a loop picker + typed-input form + payload mapping; save and confirm the catalog binding badge appears."
      - "Attempt to attach an automation whose kind is NOT in the Loop's start[] — expect a create-time 422 (and a fire-time ReasonCode); confirm it is rejected, not dropped."
      - "Confirm a workspace-scoped automation cannot target a Loop in another workspace; confirm the reviews-watch watch tag does NOT get a start-binding badge."
      - "Back-button out of the create sheet mid-step — no half-created binding."
    must_avoid:
      - "Deep automation-engine internals; focus on the binding contract as seen from the Loop."
  design_reference: "loop-detail.html (§4.2 Start bindings; §9.14) + automation create sheets"
  truthful_ui_check: "start[] allowlist enforced (422 + ReasonCode, not silent drop); schedule/webhook fires a real loop_run (never session-direct); watch tag ≠ start binding."
```

## Debrief — <YYYY-MM-DD> (<report path>)

- **Ran:** <started> → <ended> (box respected: yes/no)
- **Findings:**
- **Bugs filed/updated:**
- **Scenarios settled:**
- **Paper cuts:**
- **Surprises:**
- **Suggested next charter:**
