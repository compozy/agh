# CH-007: An author forks and edits a Loop in two tabs — does the linter gate publish and CAS stop a lost update?

```yaml
charter:
  id: CH-007
  mission: "As an author, fork software-delivery, edit its body on the canvas, and publish — and prove the shared linter (not the GUI) gates publish, positions live in the sidecar, and a two-tab race hits a 409 CAS instead of a silent overwrite."
  mode: charter-with-tour
  persona:
    name: Bruno
    device: desktop
    network: wifi-fast
    locale: en-US
  journey: J-06
  scenarios: [LP-021, LP-022, LP-023, LP-024]
  tour: Multi-Tab Tour
  time_box_minutes: 90
  guidance:
    must_try:
      - "Fork → confirm the Unsaved-changes chip and a writable draft; add a node from the palette and confirm the inspector renders from the canonical DSL types."
      - "Raise max_fan_out above 64: the node must show a danger badge, the linter chip must flip fail, Publish must be DISABLED (fan_out_ceiling_exceeded); lower it ≤ 64 and confirm Publish re-enables."
      - "Toggle Graph/DSL: the DSL view must render read-only agh.loop/v1 with the offending field highlighted; confirm node positions are NOT present in the DSL (sidecar only)."
      - "Open the same Loop in two tabs; publish in tab A; publish in tab B — tab B must hit a 409 CAS with the current version, never a silent overwrite."
      - "Publish the valid fork and run it end-to-end to a terminal outcome."
    must_avoid:
      - "The no-fork Configure sheet (CH-006)."
  design_reference: "loop-editor.html (§4.6; ADR-008/015/020/023)"
  truthful_ui_check: "shared Go linter is the sole authority; Publish disabled while issues exist; warnings never claim a 422; positions in the sidecar, never the definition; CAS 409 on stale publish."
```

## Debrief — <YYYY-MM-DD> (<report path>)

- **Ran:** <started> → <ended> (box respected: yes/no)
- **Findings:**
- **Bugs filed/updated:**
- **Scenarios settled:**
- **Paper cuts:**
- **Surprises:**
- **Suggested next charter:**
