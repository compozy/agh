# CH-005: A watch-source Loop runs unattended for a long stretch — does it conclude truthfully?

```yaml
charter:
  id: CH-005
  mission: "As an operator, run reviews-watch across several wake cycles and a long silence — and prove it rests at zero cost while watching, reports a clean tick as no-op, and ends stalled (not an infinite wait) when the source goes quiet."
  mode: scenario-based
  persona:
    name: Bruno
    device: desktop
    network: wifi-fast
    locale: en-US
  journey: J-08
  scenarios: [LP-029, LP-030, LP-031, LP-032, LP-038]
  tour: Interrupt Tour
  time_box_minutes: 90
  guidance:
    must_try:
      - "Start reviews-watch; between events confirm the live watching state is dormant (zero lease, zero token) — meters must not climb while idle."
      - "Feed a review event: verify the round (fetch → fan-out fix → resolve-check) and a truthful done when unresolved reaches zero."
      - "Feed a clean tick with nothing new: it must end no-op, rendered as its own neutral terminal — never done-with-fake-work."
      - "Withhold events past the window: the run must end stalled with an escalation ping — no infinite silent wait."
      - "Make the external dependency impossible (missing credential / refused run-loop cycle): the tick must end `blocked` — distinct from `stalled`, never coerced (LP-038; needs the AB-004 seed)."
      - "Drive the source from an extension WITH loop.watch_source (and confirm one WITHOUT it cannot serve watch/poll)."
    must_avoid:
      - "Human-gate approval (CH-002) and the converse-and-decide channel (CH-010)."
  design_reference: "run-detail.html (§4.4 rounds) + loops-catalog.html (§4.1 watch tag, cap ∞)"
  truthful_ui_check: "watching = live zero-cost dormant; no-op never coerced to done; stalled renders as itself; watch tag never gets a start-binding badge."
```

## Debrief - 2026-07-06 (`docs/qa/reports/2026-07-06-loops.md`)

- **Ran:** 2026-07-06T20:58Z -> 2026-07-06T21:45Z (box respected: yes)
- **Findings:** Reviews-watch, watch-source, stalled/no-op/blocked semantics, and extension watch-source provenance were covered by runtime E2E and extension status evidence.
- **Bugs filed/updated:** None.
- **Scenarios settled:** LP-029=pass; LP-030=pass; LP-031=pass; LP-032=pass; LP-038=pass
- **Paper cuts:** None.
- **Surprises:** The extension provenance payload is the clearest proof that watch-source capability belongs to dev-cycle.
- **Suggested next charter:** Repeat with a real authenticated gh account when testing external CodeRabbit behavior.
