# CH-004: An autonomous agent operates a Loop through structured surfaces — is web-UI-only control truly unnecessary?

```yaml
charter:
  id: CH-004
  mission: "As an autonomous agent, discover, run, and monitor a Loop to terminal using only agh__loop_* tools — and prove parity with the operator/HTTP view, deterministic output, and an enforced no-self-approval gate."
  mode: scenario-based
  persona:
    name: Ada
    device: desktop
    network: wifi-fast
    locale: en-US
  journey: J-07
  scenarios: [LP-025, LP-026, LP-027, LP-028]
  tour: Feature Tour
  time_box_minutes: 60
  guidance:
    must_try:
      - "Discover via agh__loop_list / inspect; run via agh__loop_run; diff the structured output against the same op over CLI, HTTP, and UDS — any drift is a finding."
      - "Poll agh__loop_status through a full run: the value must be one of the 11 states literally, never inferred, never coerced (exhausted/stalled/needs-approval never returned as done/failed)."
      - "Attempt to approve the agent's OWN gate — it must be rejected; confirm an operator/other agent can; confirm token redaction is hash-form-only."
      - "Call agh__loop_* before the service is ready — expect Unavailable(ReasonDependencyMissing) with a deterministic ReasonCode."
    must_avoid:
      - "Web-UI assertions (this journey is deliberately non-UI)."
  design_reference: "structured surface (no product screen); parity target run-detail.html (§4.4) / runs.html (§4.5)"
  truthful_ui_check: "structured status value IS the state (no coercion); approve capability gate holds (no self-approval); deterministic ReasonCode contracts."
```

## Debrief — <YYYY-MM-DD> (<report path>)

- **Ran:** <started> → <ended> (box respected: yes/no)
- **Findings:**
- **Bugs filed/updated:**
- **Scenarios settled:**
- **Paper cuts:**
- **Surprises:**
- **Suggested next charter:**
