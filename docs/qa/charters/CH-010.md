# CH-010: An operator watches agents converse and decide inside a run — is the decision real, and does no-decision stall?

```yaml
charter:
  id: CH-010
  mission: "As an operator, run a converse-and-decide Loop and watch the embedded channel reach a decision that drives the fan-out — and prove the harvested result is real, and a no-decision window ends stalled, never a fabricated done."
  mode: scenario-based
  persona:
    name: Bruno
    device: desktop
    network: wifi-fast
    locale: en-US
  journey: J-10
  scenarios: [LP-036, LP-037]
  tour: Feature Tour
  time_box_minutes: 60
  guidance:
    must_try:
      - "Run the hand-built converse-and-decide seed (see AB-002 — there is NO installed template); confirm the step posts via agh__network_send with a channel_result harvest declared."
      - "Watch the run-detail timeline embed the live channel conversation; confirm the harvested result is highlighted and drives the fan-out to a verified done."
      - "Run a variant where no decision is reached within the window: it must end stalled with an escalation — never a fabricated decision or coerced done."
      - "Confirm the timeline renders channel posting as agh__network_send + channel_result; the retired channel-post kind must not appear."
    must_avoid:
      - "Human-gate approval (CH-002) and watch-source rounds (CH-005)."
  design_reference: "run-detail.html (§4.4 embedded channel + harvested decision)"
  truthful_ui_check: "the UI never fabricates a decision; no decision in the window → stalled; channel posting is agh__network_send + channel_result (channel-post kind retired)."
```

<!-- Blocked-planning note: this charter cannot run until AB-002 (converse-and-decide seed harness) exists; there is no runtime-installed converse-and-decide template. -->

## Debrief — <YYYY-MM-DD> (<report path>)

- **Ran:** <started> → <ended> (box respected: yes/no)
- **Findings:**
- **Bugs filed/updated:**
- **Scenarios settled:**
- **Paper cuts:**
- **Surprises:**
- **Suggested next charter:**
