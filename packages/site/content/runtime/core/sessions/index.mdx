---
title: Sessions
description: The durable runtime unit AGH creates, resumes, audits, and governs.
---

A session is the unit AGH actually manages. It binds one agent subprocess to a workspace,
permission policy, event store, and resume record under one stable AGH session ID.

Session-scoped Vault refs use `vault:sessions/<session_id>/<name>`. They are ordinary encrypted
Vault records filtered by the session prefix, and the session inspector shows only redacted metadata
for those refs.

Use this section when a live run needs supervision or forensic detail: why it started, what state it
entered, which events were persisted, whether it can resume, and which permission checks apply while
the agent works. If the problem is choosing an agent definition, use Agents first. If the problem is
the daemon or database itself, use Operations after you identify the affected session.

<OperatorNote>
  Reach for this section when you need the operational contract around one live session: how it
  starts, what gets stored, how replay works, and how approvals are enforced while the agent runs.
</OperatorNote>

The reading order follows the lifecycle. Start with lifecycle for state names and stop
classification, move to events when you need the durable record, then use resume and permissions for
the two most common operator questions: "can this continue?" and "why did AGH ask before acting?"

<figure className="not-prose my-8 overflow-hidden rounded-lg border border-(--color-divider) bg-(--color-surface) p-2">
  <img
    src="/images/runtime/sessions-overview-storyboard-v1.png"
    alt="Hand-drawn Sessions poster showing the AGH session lifecycle across create, active, stop, resume, replay, permissions, and audit with the octopus mascot."
    loading="eager"
    decoding="async"
    className="h-auto w-full rounded-md"
  />
</figure>

## In this section

<RouteList>
  <RouteRow
    href="/runtime/core/sessions/lifecycle"
    label="Lifecycle"
    title="Create, activate, stop, and classify sessions"
    description="Use this page for the state machine, stop behavior, timeout boundaries, and what AGH persists across the session lifetime."
    meta="State model"
  />
  <RouteRow
    href="/runtime/core/sessions/resume"
    label="Resume"
    title="Resume stopped sessions and replay stored work"
    description="Use this page to understand ACP `session/load`, fresh-start fallback, transcript reconstruction, and when resume will fail."
    meta="Recovery"
  />
  <RouteRow
    href="/runtime/core/sessions/events"
    label="Events"
    title="Query, stream, and persist session events"
    description="Use this page for the event catalog, SSE behavior, SQLite storage, and the difference between persisted streams and prompt streams."
    meta="Observability"
  />
  <RouteRow
    href="/runtime/core/sessions/permissions"
    label="Permissions"
    title="Static modes, workspace boundaries, and approval flow"
    description="Use this page to understand what `deny-all`, `approve-reads`, and `approve-all` actually permit and how interactive approvals work."
    meta="Governance"
  />
  <RouteRow
    href="/runtime/core/configuration/vault"
    label="Vault"
    title="Session-scoped secret metadata"
    description="Use this page when a session needs write-only encrypted refs such as `vault:sessions/<session_id>/<name>`."
    meta="Secrets"
  />
</RouteList>
