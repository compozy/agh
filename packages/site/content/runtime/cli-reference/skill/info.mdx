---
title: "agh skill info"
description: "Show detailed metadata for one skill"
---

## agh skill info

Show detailed metadata for one skill

```
agh skill info <name> [flags]
```

### Examples

```
  # Inspect a skill's metadata and resource list
  agh skill info code-review
```

### Options

```
      --for-agent string   Resolve the effective skill set for one logical agent
  -h, --help               help for info
      --workspace string   Resolve the daemon-managed skill from a workspace id, name, or path
```

### Options inherited from parent commands

```
      --json            Emit JSON output
  -o, --output string   Output format: human, json, jsonl, or toon (default "human")
```

## Output Formats

Every AGH command supports `-o, --output`:

- `human` for interactive terminal use
- `json` for scripts and other machine-readable consumers
- `jsonl` for wait or streaming commands that emit one JSON record per line
- `toon` for compact agent-readable summaries

Example:

```bash
agh skill info <name> -o json
```
