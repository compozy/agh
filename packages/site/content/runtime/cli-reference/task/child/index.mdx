---
title: "agh task child"
description: "Manage child tasks"
---

## agh task child

Manage child tasks

```
agh task child [flags]
```

### Options

```
  -h, --help   help for child
```

### Options inherited from parent commands

```
  -o, --output string   Output format: human, json, or toon (default "human")
```

## Output Formats

Every AGH command supports `-o, --output`:

- `human` for interactive terminal use
- `json` for scripts and other machine-readable consumers
- `toon` for compact agent-readable summaries

Example:

```bash
agh task child -o json
```

## Subcommands

| Command                                                           | Description                          |
| ----------------------------------------------------------------- | ------------------------------------ |
| [agh task child create](/runtime/cli-reference/task/child/create) | Create a child task beneath a parent |
