---
title: "agh task run enqueue"
description: "Enqueue a task run"
---

## agh task run enqueue

Enqueue a task run

```
agh task run enqueue <task-id> [flags]
```

### Options

```
      --channel string           Optional run channel override
  -h, --help                     help for enqueue
      --idempotency-key string   Optional idempotency key
```

### Options inherited from parent commands

```
  -o, --output string   Output format: human, json, or toon (default "human")
```
