---
title: "agh task publish"
description: "Publish a draft task and enqueue its first run"
---

## agh task publish

Publish a draft task and enqueue its first run

```
agh task publish <id> [flags]
```

### Options

```
      --channel string           Optional run channel override
  -h, --help                     help for publish
      --idempotency-key string   Optional idempotency key
      --metadata string          Optional run metadata JSON
```

### Options inherited from parent commands

```
  -o, --output string   Output format: human, json, jsonl, or toon (default "human")
```

## Output Formats

Every AGH command supports `-o, --output`:

- `human` for interactive terminal use
- `json` for scripts and other machine-readable consumers
- `jsonl` for wait or streaming commands that emit one JSON record per line
- `toon` for compact agent-readable summaries

Example:

```bash
agh task publish <id> -o json
```
