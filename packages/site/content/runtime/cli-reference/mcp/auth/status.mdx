---
title: "agh mcp auth status"
description: "Show redacted remote MCP auth status"
---

## agh mcp auth status

Show redacted remote MCP auth status

```
agh mcp auth status [server] [flags]
```

### Options

```
  -h, --help      help for status
      --refresh   Refresh tokens before reporting status
```

### Options inherited from parent commands

```
  -o, --output string   Output format: human, json, or toon (default "human")
```

## Output Formats

Every AGH command supports `-o, --output`:

- `human` for interactive terminal use
- `json` for scripts and other machine-readable consumers
- `toon` for compact agent-readable summaries

Example:

```bash
agh mcp auth status [server] -o json
```
