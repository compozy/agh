---
title: "agh memory history"
description: "Show memory operation history"
---

## agh memory history

Show memory operation history

```
agh memory history [flags]
```

### Examples

```
  # Show recent global and current-workspace memory operations
  agh memory history

  # Filter memory writes in the current workspace
  agh memory history --scope workspace --operation memory.write --since 24h
```

### Options

```
  -h, --help               help for history
      --limit int          Maximum number of operations to return (default 25)
      --operation string   Operation type, for example memory.write
      --scope string       Memory scope: global or workspace
      --since string       Show operations since an RFC3339 timestamp or relative duration
```

### Options inherited from parent commands

```
  -o, --output string   Output format: human, json, or toon (default "human")
```

## Output Formats

Every AGH command supports `-o, --output`:

- `human` for interactive terminal use
- `json` for scripts and other machine-readable consumers
- `toon` for compact agent-readable summaries

Example:

```bash
agh memory history --scope workspace --operation memory.write -o json
```

Operation history is bounded and redacted before it is returned. Use it for audit and debugging
signals such as recent writes, deletes, searches, and reindex operations; do not treat it as a
runtime prompt context feed.
