---
title: "agh hooks runs"
description: "Show persisted hook execution history"
---

## agh hooks runs

Show persisted hook execution history

```
agh hooks runs [flags]
```

### Options

```
      --event string     Filter by hook event
  -h, --help             help for runs
      --last int         Show only the most recent N runs
      --outcome string   Filter by hook outcome
      --session string   Session ID
      --since string     Show runs since an RFC3339 timestamp or relative duration
```

### Options inherited from parent commands

```
  -o, --output string   Output format: human, json, or toon (default "human")
```
