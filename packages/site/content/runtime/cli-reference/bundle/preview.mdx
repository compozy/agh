---
title: "agh bundle preview"
description: "Preview a bundle activation without writing resources"
---

## agh bundle preview

Preview a bundle activation without writing resources

```
agh bundle preview [flags]
```

### Options

```
      --bind-primary-channel-as-default   Bind the bundle primary channel as the effective default
      --bundle string                     Bundle name
      --extension string                  Extension name
  -h, --help                              help for preview
      --profile string                    Bundle profile name
      --scope string                      Activation scope: global or workspace (default "global")
      --workspace string                  Workspace id, name, or path for workspace scope
```

### Options inherited from parent commands

```
      --json            Emit JSON output
  -o, --output string   Output format: human, json, jsonl, or toon (default "human")
```

## Output Formats

Every AGH command supports `-o, --output`:

- `human` for interactive terminal use
- `json` for scripts and other machine-readable consumers
- `jsonl` for wait or streaming commands that emit one JSON record per line
- `toon` for compact agent-readable summaries

Example:

```bash
agh bundle preview -o json
```
