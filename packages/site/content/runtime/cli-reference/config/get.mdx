---
title: "agh config get"
description: "Get one redacted effective config value"
---

## agh config get

Get one redacted effective config value

```
agh config get <path> [flags]
```

### Options

```
  -h, --help               help for get
      --workspace string   Workspace root whose overlay should be included
```

### Options inherited from parent commands

```
  -o, --output string   Output format: human, json, or toon (default "human")
```

## Output Formats

Every AGH command supports `-o, --output`:

- `human` for interactive terminal use
- `json` for scripts and other machine-readable consumers
- `toon` for compact agent-readable summaries

Example:

```bash
agh config get <path> -o json
```
