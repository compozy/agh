- Goal (incl. success criteria):
  - Audit every story under `systems-tasks-*` + `routes-app-stories-tasks*` in Storybook (`:6006`) and fix every UI defect (spacing, alignment, hierarchy, error rendering, inconsistency) so each surface conforms to `DESIGN.md` and the `impeccable` standard.
  - Each surface group passes: agh-design + impeccable audit, after-screenshot captured, lint/typecheck green for changed files.
  - Final gate: `bunx turbo run typecheck --filter=./web` + `bunx turbo run test --filter=./web` + `make bun-lint` clean.
- Constraints/Assumptions:
  - Storybook running at :6006 (`200 OK` confirmed).
  - Component sources live under `web/src/systems/tasks/components/`.
  - Subagents may write code only inside their assigned files; shared primitives (`@agh/ui`, tokens.css) MUST be reported back, not modified silently.
  - Tokens come from `packages/ui/src/tokens.css` / `DESIGN.md`; no ad-hoc hex.
  - Eyebrow contract enforced (Inter UC 11/600/-0.005em via `<Eyebrow>` or `eyebrow` utility).
  - Workspace is read-only via subagent default; this run delegates writes explicitly.
- Key decisions:
  - Split work into 5 surface groups dispatched in parallel as analysis-only first (capture + audit), then synthesize, then dispatch fix subagents.
  - Screenshot evidence required before and after for each story (agh-ui-screenshot).
  - No edits to `@agh/ui` primitives in this pass — surface back if needed.
- State:
- Done:
  - Confirmed Storybook reachable, listed 80 task stories, drafted surface groupings.
- Now:
  - Dispatching 5 parallel analysis subagents.
- Next:
  - Synthesize punch list → dispatch fix subagents → verify gates.
- Open questions (UNCONFIRMED if needed):
  - Whether `bunx turbo run test --filter=./web` is the full gate the user wants for this pass (assumed yes per web/CLAUDE.md).
- Working set (files/ids/commands):
  - `web/src/systems/tasks/components/*.tsx`
  - Story IDs grouped in `.tmp/tasks-ui-fix/audits/`
  - DESIGN.md, web/CLAUDE.md, .agents/skills/agh-ui-screenshot
