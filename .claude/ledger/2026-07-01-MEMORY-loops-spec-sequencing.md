# MEMORY — loops-spec-sequencing

- Goal (incl. success criteria): From handoff /tmp/handoff-hermes-loops-2026-07-01.md: (1) DONE — decided new sibling spec goes BEFORE loops implementation (no hard dependency either way; foundational + fixes real gaps). (2) DONE — loops spec inclusions limited to: optional 5-field contract DSL augmentation, evidence-required judge prose, verify ADR-007 covers WAIT-barrier. Session-continuity stays a separate sibling spec. NOW: author TechSpec for candidate 1 (typed-block taxonomy et al.) via cy-spec-preflight + cy-create-techspec after grill-me interview.
- Constraints/Assumptions: Two-touch rule protects loops spec (6 peer-review rounds). Conversation BR-PT, artifacts EN. SQLite numbered append-only migrations; ClaimNextRun sole claim authority; workspace_id up front; config.toml single slot; agent-manageable via CLI/HTTP/UDS; greenfield hard cuts.
- Key decisions (grill-me interview, all confirmed by user):
  - Scope: core (typed blocks, auto-resume, breaker, TaskBlocked/TaskUnblocked hooks) + needs_attention CHECK fix + hallucination gate + wake-creator-agent.
  - Model: new task_blocks table, kind ∈ {needs_input, capability, transient}; blocked derives from deps ∨ approval ∨ open blocks; unified blocked_reasons read view.
  - Auto-resume: AutoEnqueueOnReady stays opt-in; extended triggers (block-clear, approval→approved) share the idempotent enqueue path.
  - Breaker: counter per task+kind, config.toml limit default 2, reset only on successful completion; at limit → TASK-LEVEL needs_attention (new task status) + hook + event.
  - Wake-creator: default-on for agent_session-created tasks; events terminal + block-created + needs_attention; synthetic queued turn (fresh, idle); suppress dead session / loop-owned / per-task opt-out.
  - Hallucination gate: strict on structured created_task_ids (reject completion, typed error, audit, run stays running) + advisory prose scan (warning only).
  - Block mid-run: agh\_\_task_block = block + release lease in one tx; run → queued, gated; no attempt consumed.
- Key runtime facts (verified by Explore agents):
  - autoEnqueueReadyDependents EXISTS: internal/task/lease_manager.go:227-271, fires in CompleteRunLease (line 212), opt-in via AutoEnqueueOnReady, DERIVED-ready check.
  - Task status DERIVED on read (canonicalTaskStatusReadOnlyWithStore manager.go:2770; taskStatusFromPolicySnapshot :2839); stored status reconciled via reconcileTaskCascade (:2718); ClaimNextRun tests STORED tasks.status (global_db_task_claim.go:517,528).
  - CORRECTED (peer review r1): NO live bug on task*runs — migration v39 (drop_task_run_status_check, migrate_task_run_status.go:30-87) already removed the task_runs status CHECK; MarkRunNeedsAttention persists fine. The REAL gap: tasks.status CHECK is still live (global_db.go:564-568 + migrate_workspace.go:511-591) and omits needs_attention → spec now includes a tasks CHECK-drop migration (mirror v39). Base DDL in global_db.go is pre-migration shape — never cite it as current schema without checking migrate*\*.go.
  - Creator linkage exists: tasks.created_by_kind/ref; agent_session → ref = session id (native_tools.go:2687-2705, actorContextFromScope :5532). No created-by query filter/index yet (Query struct types.go:810-822).
  - Synthetic turn infra exists: TurnSourceSynthetic (session/interfaces.go:22-29), PromptSyntheticMeta (acp/types.go:231-243 — TaskID/TaskRunID/WakeEventID/Reason/Summary), busy-input queue (manager_busy_input.go). Missing: task-event → session wake listener.
  - Completion payload: LeaseCompletion{RunID, ClaimToken, Result RunResult{Value json.RawMessage}} (lease.go:108-113); no structured summary/created-tasks field.
  - Task hooks today: 9 run-centric Dispatch\* (hooks.go:10-47); no TaskBlocked/TaskUnblocked.
  - Hermes reference: VALID_BLOCK_KINDS {dependency, needs_input, capability, transient}; dependency→todo auto-promote; block_recurrences limit 2 → triage, reset only on completion; wake on {completed, gave_up, crashed, timed_out, blocked} via internal MessageEvent (01_analysis_kanban-model.md:28-38).
- State: PEER REVIEW ROUND 3 INCORPORATED (r3 verdict NEEDS_REWORK narrowed to delete-list precision: taskTableIndexStatements is base-DDL-shared → struck from delete list; migrateTaskTables siblings migrateTaskRunMetadataColumn/migrateTaskEventSequenceColumn → deleted with grep-verified base-DDL coverage; tableDefinitionSQL orphan deleted; shared helpers kept; no hardcoded index count; double-boot test extended). r3 confirmed r2's migration plan closed. Markers OK after every round. Prior state: ROUND 2 INCORPORATED. Round 1 (NEEDS_REWORK: stale v39 claim, tasks.status CHECK gap) and round 2 (NEEDS_REWORK: live boot reconciler migrateTaskTables would resurrect the CHECK/drop new columns on next boot; UpConn/non-UpConn phase split diverges fresh-vs-upgrade) both fully incorporated (qa/peer-review-incorporation-round{1,2}.md). Final shape: v48 single UpConn tasks rebuild + reconciler machinery DELETED + v49 UpConn blocks tables; loop-suppression seam removed; dotted event names. Markers OK. Awaiting: round 3 or stop.
- KEY MIGRATION FACTS (verified r2): fresh-DB bootstrap runs non-UpConn migrations phase 1 then UpConn phase 2 (registry order ≠ execution order for mixed kinds); boot reconciler chain runs BEFORE RunMigrations on non-fresh DBs; taskTableNeedsRebuild keys on the 'draft' literal (only present in the status CHECK); registry tail = v47 add_task_run_designations.
- Done: explore (3 agents) answering handoff Q1/Q2; sequencing verdict delivered; grill-me 7 questions + 2 post-preflight questions (transient expires_at lazy+sweep; needs_attention columns on tasks + `task recover` verb); cy-spec-preflight full read; 7 ADRs; techspec draft approved option A and saved; markers OK.
- Now: PEER REVIEW CLOSED after 3 rounds (user chose stop; all findings incorporated each round). TechSpec is final at .compozy/tasks/task-blocks/\_techspec.md with ADRs 001-007 and full qa/ round artifacts.
- Next: cy-create-tasks to generate \_tasks.md for task-blocks (remember cy-spec-preflight tasks-phase checks + cy-tasks-tail-qa-pair + cy-web-docs-impact).
- LOOPS INCLUSIONS: DONE (user-directed, applied directly 2026-07-02). New ADR-018 in .compozy/tasks/loops/adrs/ (optional contract fields constraints/boundaries/stop_when rendered to worker+judge; evidence-required approved verdicts — approved+empty evidence deterministically → rejected, never Broken; WAIT verdict considered-and-rejected: no polling site exists, watch-source/needs-approval/running-node cover async waits). Co-shipped: \_techspec.md (DSL block + rendering paragraph + gate bullet + ADR index + unit lane 36→38 cases + "ADR-001..018"), \_tests.md (unit-37/38 appended, NO renumbering), task_02 (parse fields + owns 37-parse-half), task_08 (rubric+evidence requirement + subtask 8.6 + owns unit-38), task_09 (worker render requirement + owns 37-render-half), \_tasks.md header.
- Open questions (UNCONFIRMED): none blocking this spec.
- Working set (files/ids/commands):
  - Spec target: .compozy/tasks/<slug>/\_techspec.md (slug TBD, suggest task-blocks)
  - Handoff: /tmp/handoff-hermes-loops-2026-07-01.md; prior ledger: .claude/ledger/2026-07-01-MEMORY-hermes-orchestration-research.md
  - Hermes analyses: .compozy/tasks/hermes-orchestration-research/analysis/{summary,01..05}\*.md
  - Runtime: internal/task/{types,manager,lease_manager,lease,hooks,starvation_events}.go, internal/store/globaldb/{global_db,global_db_task,global_db_task_claim}.go, internal/scheduler/starvation.go, internal/config/autonomy.go, internal/session/manager_busy_input.go, internal/acp/types.go, internal/daemon/native_tools.go
