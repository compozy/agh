# Task Memory: task_08.md

Keep only task-local execution context here. Do not duplicate facts that are obvious from the repository, task file, PRD documents, or git history.

## Objective Snapshot
- Implement Task 08: public contract DTOs, shared HTTP/UDS handlers and routes, agent-native UDS channel semantics, OpenAPI/web generated contract artifacts, and parity/legacy-rejection tests for network conversation containers.
- Success requires strict hard-cut rejection of `interaction_id` and `kind:"direct"`, HTTP/UDS parity for thread/direct/work routes, direct resolve over both transports, `make codegen` plus `make codegen-check`, clean `make verify`, tracking updates, and one local commit after verification.

## Important Decisions
- Use `_techspec.md` route map and payload shapes as source of truth: retained status/peers/channels/send/inbox, deleted primary flat channel/peer message paths, new thread/direct/work read paths, and direct resolve.
- Scope stays on public contracts and transport parity; CLI/native tools/extensions/web UI implementation are later tasks unless generated types or compile fallout require minimal alignment.
- Existing dirty task-tracking/QA deletions and unrelated untracked ledgers/memory were present before implementation; do not revert or stage unrelated files.

## Learnings
- Shared memory says Tasks 05-06 provide durable conversation repository and runtime routing APIs; Task 08 should consume those instead of adding new persistence/runtime primitives.
- Task 07 exposes aggregate network status counters; Task 08 may surface only aggregates in public status payloads if needed.
- Pre-change signal: `internal/api/contract/contract.go`, `openapi/agh.json`, and `web/src/generated/agh-openapi.d.ts` still expose `interaction_id`; `NetworkSendRequestFromPayload` maps the legacy field into runtime `WorkID`; HTTP/UDS routes still expose old flat channel/peer message paths and lack thread/direct/work routes.
- Current agent-native UDS already uses `surface:"thread"` for channel send and `surface:"direct"` for replies, but reply fallback still reads persisted flat messages through `ListNetworkMessages`; Task 08 should preserve no-legacy semantics and strengthen tests.
- Implementation draft now consumes `store.NetworkConversationStore` through `core.NetworkStore`, adds shared core handlers for thread/direct/work reads and direct resolve, and uses deterministic `network.DirectRoomIdentity` when resolving direct rooms from a local session and target peer.
- Public HTTP/UDS route registration now removes primary flat `/peers/:peer_id/messages` and `/channels/:channel/messages` paths and adds the TechSpec thread/direct/work route map. Old flat handlers remain reachable only through core test helper routes to preserve existing non-public handler coverage.
- Tests added/drafted cover contract JSON hard-cut rejection, send surface/container/work validation, conversation read paths, HTTP/UDS direct resolve, and UDS network route parity against documented HTTP/UDS spec operations.
- `make codegen`/`make codegen-check` are clean; generated `openapi/agh.json` and `web/src/generated/agh-openapi.d.ts` no longer contain `interaction_id` or the removed `listNetworkChannelMessages`/`listNetworkPeerMessages` operations.
- First full `make verify` found existing web network consumers still typed against those removed operations. Scoped web alignment keeps component-facing timeline types intact but backs channel messages through `listNetworkThreads` + `listNetworkThreadMessages`, and peer messages through `getNetworkPeer` + `listNetworkDirectRooms` + `listNetworkDirectRoomMessages`; `make web-typecheck` passes.
- Full verify then reached lint/typecheck and found daemon fallout: `daemon.Registry` needed to expose `store.NetworkConversationStore`, the shared daemon `recordingRegistry` test double needed conversation-store stubs, and native network send still accepted/build `interaction_id`. Native send input now accepts `surface`, `thread_id`, `direct_id`, and `work_id` and converts through the shared validator.
- The following lint/typecheck pass found CLI fallout: `network send` still registered `--interaction-id` and rendered `interaction_id`. Updated the command to expose `--work-id`, `--surface`, `--thread-id`, and `--direct-id`; default CLI and daemon packages compile against the new generated contract.
- Lint then reported mechanical issues from the added public surfaces: CLI send exceeded `funlen`, new `StubNetworkStore` callback fields exceeded line length, and the shared positive-int query parser accepted a key that is always `limit`. Split CLI flag registration, wrapped callback types, and fixed the parser to the `limit` query key; focused package lint is clean.
- Native tool verification exposed the builtin `network_send` JSON schema as the last stale ingress guard: it still accepted `interaction_id` and rejected `surface`/container/work fields. Updated the descriptor schema to allow `surface`, `thread_id`, `direct_id`, and `work_id` and remove `interaction_id`; focused daemon native send, builtin descriptor, and bundled skill content tests pass.
- First post-fix `make verify` passed frontend format/typecheck and reached the frontend test suite, then failed only because `sdk/typescript/src/integration.test.ts` hit its fixed 30s timeout under full-suite load. The exact focused integration test passed immediately afterward (`2 passed`, 292ms), so rerun full verification for a clean gate instead of changing unrelated test code.
- Verification evidence: `make codegen-check` passed; focused `go test ./internal/api/contract ./internal/api/core ./internal/api/httpapi ./internal/api/udsapi ./internal/tools/builtin ./internal/daemon ./internal/skills/bundled -count=1` passed; focused timeout checks `sdk/typescript/src/integration.test.ts` and `packages/site/lib/static-route-metadata.test.ts` passed in isolation; final full gate after all code changes `VITEST_MAX_WORKERS=4 make verify` exited 0 with frontend format/lint/typecheck/tests/build, Go lint (`0 issues`), Go tests (`DONE 8272 tests`), and package boundary checks (`OK: all package boundaries respected`).
- Task tracking updated: `task_08.md` is marked `completed` with all subtasks and test checklist items checked; `_tasks.md` now includes task 08 as completed while preserving pre-existing unrelated tracking edits.
- Final self-review found stale legacy vocabulary in a build-tagged daemon network collaboration integration test and web MSW/story fixtures. Updated those surfaces to use direct rooms as `surface:"direct"` plus `direct_id`/`work_id`, reject legacy mock send input, and read thread messages through the new thread routes. Targeted `make web-typecheck`, `go test -tags integration ./internal/daemon -run '^$' -count=0`, and daemon assertion tests pass after that correction.
- Local implementation commit created: `eb30d28d feat: expose network conversation contracts`. Tracking/memory files and pre-existing unrelated QA/task edits remain unstaged per task instruction to keep tracking-only files out of automatic commits.

## Files / Surfaces
- Expected touch surfaces: `internal/api/contract/contract.go`, `internal/api/core/interfaces.go`, `internal/api/core/network.go`, `internal/api/core/network_details.go`, `internal/api/core/agent_channels.go`, `internal/api/httpapi/routes.go`, `internal/api/udsapi/routes.go`, `internal/api/spec/spec.go`, `openapi/agh.json`, `web/src/generated/agh-openapi.d.ts`, and focused API tests.
- Additional touched surfaces so far: `internal/api/contract/responses.go`, `internal/api/core/network_conversations.go`, `internal/api/core/errors.go`, `internal/api/core/test_helpers_test.go`, `internal/api/testutil/apitest.go`, `internal/api/httpapi/network_test.go`, `internal/api/httpapi/handlers_test.go`, `internal/api/udsapi/network_test.go`, `internal/api/udsapi/handlers_test.go`, `web/src/systems/network/types.ts`, `web/src/systems/network/adapters/network-api.ts`, `web/src/systems/network/lib/query-keys.ts`, `internal/tools/builtin/network.go`, `internal/daemon/native_tools.go`, `internal/daemon/native_tools_test.go`, `internal/skills/bundled/bundled_test.go`, and `internal/skills/bundled/skills/agh-network/SKILL.md`.

## Errors / Corrections
- Correction: public flat message routes were removed from HTTP/UDS registration, but core unit-test helper routes were restored for existing internal handler tests; transport route tests guard the public hard cut.
- Correction: web typecheck required minimal consumer alignment despite Task 13 owning the full web UX. Kept the change at the system API/type layer instead of redesigning components.
- Correction: daemon/native-tool compile fallout was fixed at the composition boundary by widening the registry interface to the already-implemented conversation store and removing the native send `interaction_id` input field.
- Correction: CLI compile fallout was fixed as a hard-cut rename, not an alias; `--interaction-id` is no longer registered.
- Correction: the shared `parsePositiveIntQuery` helper now parses only the `limit` query because every current call site used that key and lint rejected the unused parameter.
- Correction: native tool input validation had to be updated at the builtin descriptor schema as well as the daemon handler; otherwise the shared handler never received the new send fields.
- Correction: do not weaken the unrelated extension SDK timeout; require a clean full verify rerun after the focused test shows the integration path itself is healthy.
- Correction: full frontend Vitest needed the supported `VITEST_MAX_WORKERS=4` bound in this local run because default all-worker execution repeatedly starved unrelated timeout-sensitive tests despite those tests passing in isolation.
- Correction: build-tagged integration fixtures are part of the hard-cut surface even when default verification does not execute them; keep them compiling against the new route and CLI contract.

## Ready for Next Run
