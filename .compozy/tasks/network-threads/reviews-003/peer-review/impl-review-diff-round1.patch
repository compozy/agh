diff --git a/web/src/systems/network/components/directs/direct-room.test.tsx b/web/src/systems/network/components/directs/direct-room.test.tsx
index 5ec05a57..69ef5340 100644
--- a/web/src/systems/network/components/directs/direct-room.test.tsx
+++ b/web/src/systems/network/components/directs/direct-room.test.tsx
@@ -109,6 +109,9 @@ describe("DirectRoom headerless layout", () => {
     expect(screen.getByTestId("network-direct-room-error")).toHaveTextContent(
       "Direct room unavailable"
     );
+    expect(screen.getByTestId("network-direct-room-error")).toHaveTextContent(
+      "Could not load direct room direct_test. Choose an existing direct room from #ops."
+    );
     expect(screen.queryByRole("textbox", { name: /message @peer/i })).toBeNull();
   });
 
diff --git a/web/src/systems/network/components/directs/direct-room.tsx b/web/src/systems/network/components/directs/direct-room.tsx
index 1e8ea5a7..79c08c4e 100644
--- a/web/src/systems/network/components/directs/direct-room.tsx
+++ b/web/src/systems/network/components/directs/direct-room.tsx
@@ -83,7 +83,7 @@ export function DirectRoom({ channel, directId, selfPeerId }: DirectRoomProps) {
       {detailError ? (
         <div className="flex flex-1 items-center justify-center px-5 py-10" role="alert">
           <ConversationError
-            description={`AGH could not load ${directId}. Choose an existing direct room from #${channel}.`}
+            description={`Could not load direct room ${directId}. Choose an existing direct room from #${channel}.`}
             testId="network-direct-room-error"
             title="Direct room unavailable"
           />
