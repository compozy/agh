# Workflow Memory

Keep only durable, cross-task context here. Do not duplicate facts that are obvious from the repository, PRD documents, or git history.

## Current State

- Task 01 (design tokens + fonts) complete on branch `pn/ui-v2`
- Task 02 (custom sidebar) complete on branch `pn/ui-v2`
- Task 04 (skills HTTP endpoints) complete on branch `pn/ui-v2`
- Task 05 (skill frontend system) complete on branch `pn/ui-v2`
- New token system: `--color-*` custom properties in `:root`, hex values, no OKLCH/color-mix
- Font stack: Inter Variable (sans) + JetBrains Mono (mono), no display font
- Flat depth model active: no shadows, no gradients, no textures in styles.css
- Custom sidebar replaces shadcn Sidebar: 40px icon rail + 220px collapsible panel
- Sidebar state managed by Zustand store at `web/src/stores/sidebar-store.ts`

## Shared Decisions

- DESIGN.md tokens live in `:root` as `--color-canvas`, `--color-accent`, etc. Components reference via `var(--color-*)`.
- shadcn theme variables (`--background`, `--primary`, etc.) use direct hex values, mapped to Tailwind utilities via `@theme inline`.
- Badge tints use 8-digit hex for 15% opacity (e.g., `#E8572A26`).
- `--radius: 0.5rem` (8px base) per DESIGN.md.
- SidebarProvider/SidebarInset removed from `_app.tsx` — layout now uses plain flex.
- Empty route files created for `/_app/skills` and `/_app/knowledge` (placeholder content).

## Shared Learnings

- `@theme inline` (Tailwind v4) does NOT create CSS custom properties — no collision with `:root` definitions of same name.
- oxfmt lowercases hex values — tests must use case-insensitive regex.
- TanStack Router route tree auto-generated by Vite plugin; manual gen: `npx @tanstack/router-cli generate`.

## Open Risks

- shadcn ui/ components (popover, dropdown, etc.) still have `shadow-*` Tailwind utilities — acceptable for now, may need flat-depth pass in future tasks.

## Handoffs

- Task 03 (session chat restyle) depends on task 01 tokens + task 02 sidebar — both done.
- Task 06 (skills page) will build on the `systems/skill/` data layer from task 05 and the empty `/_app/skills` route from task 02.
- Task 07/08 (knowledge page) will build on the empty `/_app/knowledge` route created in task 02.
- Components still use `text-[color:var(--color-text-tertiary)]` pattern (arbitrary Tailwind values). Future tasks may want to define shorthand Tailwind utilities via `@theme inline` for frequently used tokens.
- Skills enable/disable endpoints are stubs — the Registry doesn't yet have mutation methods. Future work should add `Enable(name)` / `Disable(name)` to the registry.
