# Task Memory: task_12.md

Keep only task-local execution context here. Do not duplicate facts that are obvious from the repository, task file, PRD documents, or git history.

## Objective Snapshot
- Implement Task 12: add agent-operable CLI operator commands for the Tool Registry using Task 11 HTTP/UDS contracts.
- Required command families: `agh tool list/search/info/invoke` and `agh toolsets list/info`; preserve existing `agh mcp auth` as MCP credential management.
- Completion gate: focused CLI/API tests plus full `make verify`, self-review, task tracking updates, and one local commit.

## Important Decisions
- Use `internal/cli` as a thin client over UDS/HTTP contract DTOs; do not duplicate registry logic or MCP auth flows in CLI.
- Treat generated CLI docs as Task 14 output; Task 12 should add source/tests that fail when generated docs do not include the new command pages after `make cli-docs`.
- Do not add approval-token or auth-management flags under `agh tool`; approval/auth diagnostics are surfaced through structured tool errors, while credential management remains `agh mcp auth`.

## Learnings
- Task 11 completed `/api/tools`, `/api/tools/search`, `/api/tools/{id}`, `/api/tools/{id}/approvals`, `/api/tools/{id}/invoke`, `/api/sessions/{id}/tools`, `/api/sessions/{id}/tools/search`, `/api/toolsets`, and `/api/toolsets/{id}` over HTTP and UDS.
- CLI docs are generated by `make cli-docs` from Cobra output into `packages/site/content/runtime/cli-reference`; generated files must not be hand-edited.
- Baseline: `internal/cli/tool.go` currently exposes only hidden internal `agh tool mcp`; `agh tool list -o json` renders hidden parent help, and `agh toolsets list -o json` is an unknown command.
- `internal/cli/client.go` currently parses generic `{"error":"..."}` daemon errors only; Task 12 needs structured `contract.ToolErrorResponse` parsing/rendering for tool registry failures.
- New CLI output redaction needs to handle daemon error details, result preview, structured JSON, content text/data, and metadata; JSON object keys containing token/secret/password/auth/pkce are redacted recursively.
- Package-level CLI coverage target required focused tests for new tool/toolset surfaces plus adjacent existing CLI helpers; current `go test ./internal/cli -coverprofile=/tmp/agh-task12-cli.cover -count=1` reports 80.4%.

## Files / Surfaces
- Implementation surfaces touched: `internal/cli/root.go`, `internal/cli/client.go`, `internal/cli/client_tools.go`, `internal/cli/tool.go`, `internal/cli/tool_operator.go`.
- Test surfaces touched: `internal/cli/tool_test.go`, `internal/cli/tool_integration_test.go`, `internal/cli/client_test.go`, `internal/cli/doc_test.go`, `internal/cli/helpers_test.go`, `internal/cli/config_test.go`, `internal/cli/mcp_auth_test.go`.

## Errors / Corrections
- Initial focused CLI coverage was 77.5%, then 78.6%, then 79.x after new tool tests; added targeted client/config/MCP helper coverage to reach 80.4% without weakening assertions.
- `sanitizeToolInvokeResponse` initially redacted only top-level preview/structured fields; corrected to recursively redact result/content metadata and nested structured JSON values.
- UDS integration comparison needed canonical JSON equivalence instead of raw string equality because transport encoding order is not a contract.

## Ready for Next Run
- Task 12 implementation is complete and committed locally as `9a19515e feat: add tool registry cli commands`.
- Passed:
  - `go test ./internal/cli -count=1`
  - `go test ./internal/cli -coverprofile=/tmp/agh-task12-cli.cover -count=1` (80.4%)
  - `go test -tags integration ./internal/cli -run TestCLIToolCommandsMatchUDSContractsIntegration -count=1`
  - `go test ./internal/daemon -run TestToolRoutesStayHTTPAndUDSBehaviorallyAligned -count=1`
  - `make verify` (format/oxlint/typecheck, Vitest 257 files / 1838 tests, web build, golangci-lint 0 issues, Go race tests 6966, boundaries OK)
- First `make verify` run failed on goconst/gocritic in new CLI code; fixed constants and large parameter, then full rerun passed.
- Fresh pre-commit `make verify` at 2026-04-29 04:04:51 -03 passed with exit code 0: oxlint 0 warnings/errors, Vitest all tests passed, web build completed with existing Vite chunk-size warning, golangci-lint 0 issues, Go race tests 6966, boundaries OK.
- Post-commit `make verify` at 2026-04-29 04:07:08 -03 passed with exit code 0: oxlint 0 warnings/errors, Vitest all tests passed, web build completed with existing Vite chunk-size warning, golangci-lint 0 issues, Go race tests 6966, boundaries OK.
- Task tracking updated: `task_12.md` completed/checklisted and `_tasks.md` row 12 completed.
