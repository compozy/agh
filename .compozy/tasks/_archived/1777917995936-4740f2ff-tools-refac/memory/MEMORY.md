# Workflow Memory

Keep only durable, cross-task context here. Do not duplicate facts that are obvious from the repository, PRD documents, or git history.

## Current State

## Shared Decisions

## Shared Learnings
- Current branch contains the `tools-registry` foundation needed by follow-up built-ins: `internal/tools` has registry dispatch/projection/policy/approval contracts and daemon native provider wiring. Older notes about missing `Registry.Call`/projection contracts are stale for this branch.
- Task 01 dynamic tool policy resolution is implemented in commit `a4601294`: registry projection/dispatch now resolve `PolicyInputs` through a scoped resolver, and daemon wiring applies default `agh__bootstrap`/`agh__catalog` discovery from current agent/session/workspace state.
- Task 02 guidance layer teaches the canonical discovery loop through bundled `agh-tools-guide` and startup section `tools` (`HarnessPromptSectionTools`), ordered after skills and before network when `[tools].enabled` is true. Later tool-family tasks can rely on startup guidance that prefers `agh__tool_search -> agh__tool_info -> invoke` and treats CLI as management/fallback.
- Task 03 coordination/session/workspace read surfaces are implemented in native tools: `agh__coordination` now includes network status/channels/inbox, and new `agh__sessions`/`agh__workspace` toolsets cover read-only session/workspace inspection using existing service projections.
- Config and hook mutable tool work is implemented together in code: built-in config/hook descriptors, native daemon handlers, deterministic denial reasons, config path policy helpers, hook `enabled` semantics, and generated contract updates now exist. On the current untracked PRD bundle this maps to split `task_05.md` and `task_06.md`, even though the run prompt called it Task 04.
- Real-Scenario QA fixed the autonomy hard cut: raw task-run claim tokens are internal lease state only; public CLI/API/UDS/native-tool/docs/generated surfaces use session-bound `run_id` contracts and expose at most `claim_token_hash`.
- Real-Scenario QA also fixed native read-surface parity: memory, observe, and bridge built-in descriptors now have daemon native handlers, so future tool-family additions must keep descriptor registration and native provider handling in sync.
- Task 04 memory/observe/bridge read surfaces are implemented in commit `eb2a9253`: `agh__memory`, `agh__observe`, and `agh__bridges` expose read-only inspection through existing memory store, observer, bridge service, and bridge health projections with claim-token redaction and bridge provider-config omission.
- Task 10 implementation preserves `token_present` as a public boolean diagnostic in tool results while still redacting configured secret/token fields. Future auth/tool diagnostics should use exact allow-listed public diagnostic names instead of broad token-like field names.
- Task 11 docs alignment: hand-authored `packages/site/content/runtime/cli-reference/index.mdx` and `meta.json` survive `make cli-docs` regeneration and must be edited explicitly when new top-level CLI groups appear; `oxfmt` re-aligns markdown tables after generation, so always run `bun run format` after `make cli-docs`.
- Task 12 produced the canonical QA dossier under `.compozy/tasks/tools-refac/qa/` (5 plan files + 33 test cases + verification-report scaffold). Future QA work for `tools-refac` consumes this dossier directly; do not regenerate equivalents in another path.
- Task 13 current-branch QA exposed three reusable integration constraints: hosted MCP executable identity must tolerate case-preserved macOS path aliases via file identity, MCP dynamic discovery must isolate auth-blocked remote sources without hiding builtin tools, and daemon native tools that depend on late-boot managers must resolve those managers dynamically instead of capturing nil boot-time dependencies.

## Open Risks
- `.compozy/tasks/tools-refac/_tasks.md` may lag individual task files for task_02/task_03 status; verify both task files and code evidence before treating dependency status as blocking.
- The untracked `tools-refac` task files in this workspace do not match the run prompt numbering: on-disk `task_04.md` is memory/observe/bridge read surfaces, while the prompt's Task 04 is config/hook mutation.
- Tracking still lags implementation evidence: `.compozy/tasks/tools-registry/_tasks.md` marks tasks 13 and 14 pending even though Real-Scenario QA consumed the registry foundation; `.compozy/tasks/tools-refac/_tasks.md` also marks several implemented follow-up tasks pending.
- Full `make test-integration` is not a clean branch-wide gate as of 2026-04-30: observed failures outside Task 05 include `internal/observe` build drift around `manager.ApproveTask`, network delivery timeouts, dynamic tool resource shape mismatch, and E2E prompt/diagnostic expectation drift. Use focused task integration evidence plus `make verify` until those failures are fixed.

## Handoffs
- Task 01 committed locally as `a4601294 feat: add dynamic tool policy resolver`; post-commit `make verify` passed. Tracking/memory artifacts remain uncommitted per workflow guidance.
- Config/hook mutable tool implementation is committed locally as `0b879ef1 feat: add config and hook management tools`; post-commit `make verify` passed. Tracking/memory artifacts remain uncommitted per workflow guidance.
- Task 05 verification-only rerun on 2026-04-30 confirmed config mutable tools remain complete: focused config/native/CLI tests passed, `internal/config` package coverage was 81.5%, and full `make verify` passed. No code commit was created because no production code changed in this rerun.
- Task 06 hook-management verification added native tool read-surface coverage and `HookSourceSkill` immutability coverage in commit `b81143e7 test: cover hook management tools`; post-commit `make verify` passed with Go lint `0 issues`, `DONE 7041 tests`, and package boundaries respected.
- Task 03 read-surface implementation is committed locally as `d5316f5b feat: add coordination session workspace read tools`; post-commit `make verify` passed. Tracking/memory artifacts remain uncommitted per workflow guidance.
- Task 04 memory/observe/bridge read-surface implementation is committed locally as `eb2a9253 feat: add memory observe bridge read tools`; post-commit `make verify` passed with `0 issues`, `DONE 7040 tests`, and `OK: all package boundaries respected`. Tracking/memory artifacts remain uncommitted per workflow guidance.
- Task 07 automation tool-family implementation is committed locally as `06880bab feat: add automation management tools`; it adds the `agh__automation` native toolset over the existing automation manager for jobs, triggers, manual trigger, and run history/inspection. Focused integration coverage uses real `automation.Manager` + `globaldb`; pre-commit and post-commit `make verify` passed with `0 issues`, `DONE 7059 tests`, and package boundaries respected. Tracking/memory artifacts remain uncommitted per workflow guidance.
- Task 08 extension lifecycle tool-family implementation is committed locally as `5735b42c feat: add extension lifecycle tools`; it adds the `agh__extensions` native toolset for search/list/info/install/update/remove/enable/disable over the existing extension manager, registry, managed install, marketplace/local source, approval, and reconciliation paths. Focused evidence covered lifecycle parity, deterministic source denial, approval gating, and rollback; pre-commit and post-commit `make verify` passed with `0 issues`, `DONE 7070 tests`, and package boundaries respected. Tracking/memory artifacts remain uncommitted per workflow guidance.
- Task 09 session-bound autonomy implementation is committed locally as `1119d6e4 feat: add session-bound autonomy tools`: `agh__autonomy` native tools route claim/heartbeat/complete/fail/release through the existing task lease writers, public CLI/API/UDS/OpenAPI/web/docs contracts no longer accept or return raw `claim_token`, and pre/post-commit `make verify` passed with Go lint `0 issues`, `DONE 7080 tests`, and package boundaries respected. Tracking/memory artifacts remain uncommitted per workflow guidance.
- Task 10 MCP auth status and hosted MCP parity implementation is committed locally as `5fa9f805 feat: add mcp auth status tool`: it adds `agh__mcp_auth_status` over the existing redacted MCP auth status provider, keeps login/logout management-only, preserves hosted MCP session projection/approval bridge behavior, and post-commit `make verify` passed with lint `0 issues`, Vitest `257` files / `1838` tests, Go `DONE 7094 tests`, and package boundaries respected. Tracking/memory artifacts remain uncommitted per workflow guidance.
- Task 11 docs/codegen alignment landed locally: regenerated CLI reference (added `tool/` and `toolsets/` groups), updated runtime core docs for tool-first discovery defaults, tool-callable hook/automation/extension/config lifecycles, autonomy reason codes, status-only MCP auth, and added `packages/site/lib/runtime-tools-canonical-docs.test.ts` regression coverage. Pre/post-commit `make verify` passed with oxlint 0/0, golangci-lint `0 issues`, Go `DONE 7094 tests`, site `vitest 49 tests`, and package boundaries respected.
- QA planning artifacts for `tools-refac` (118 manual TCs + 5 plan/regression docs + traceability matrix) live under `.compozy/tasks/tools-refac/qa/`. The next QA task (Real-Scenario QA Execution — `task_13.md` on-disk / prompt `task_10`) consumes them with no path changes. Use `qa-output-path=.compozy/tasks/tools-refac` to match.
- Prior Real-Scenario QA commit object `8c5d78d7 feat: harden tools refac QA surface` exists locally but is not on current branch `tools-registry` as of the task_13 rerun; it was authored on ancestor `d5316f5b`. Treat it as historical evidence only, not as a patch to apply wholesale, because current `HEAD` includes later task 04-12 work.
- Task 13 rerun on current branch produced fresh QA evidence under `.compozy/tasks/tools-refac/qa/`, fixed `BUG-001` through `BUG-006`, and passed `make test-e2e-runtime` plus final `make verify` before tracking closeout. Check `.compozy/tasks/tools-refac/qa/verification-report.md` for lab coordinates and evidence paths.
