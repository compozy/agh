# QA Issues Directory

`qa-execution` writes reproduced bug reports here as `BUG-*.md` files using the shared
`qa-report` issue template. Task 31 creates the directory and keeps it empty because no execution
bugs have been reproduced during planning.

