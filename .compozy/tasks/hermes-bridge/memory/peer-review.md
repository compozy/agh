# Implementation Peer Review Memory

## Round 1

- Verdict: `FIX_BEFORE_SHIP`.
- Findings source: `.peer-reviews/hermes-bridge-20260713T054134Z/impl-review-findings-round1.md`.
- Mandatory scope: remediate B-001..B-005 and N-001..N-008 in this iteration; validate every risk and fix each locally confirmed correctness, security, durability, or cleanup defect before the next review.
- Verification policy: focused owner suites during remediation; one workflow-wide `make verify` only after the source freeze, followed by the next implementation review round.

## Round 2

- Verdict: `SHIP` with 0 blockers, 4 non-blocking risks, and 2 nits.
- Findings source: `.peer-reviews/hermes-bridge-20260713T054134Z/impl-review-findings-round2.md`.
- Contract parity: confirmed across the Hermes TechSpec/test/QA context, the standalone `BUG-20260712-reasoning-evidence-attribution` TechSpec/ADR, round-1 findings/remediation, the provider wire leaf, all public bridge routes, the declarative bridge schema plus `internal/store/globaldb/migrations/00002_schema.sql`, generated contracts, QA report, and official docs/skill surfaces.
- Accepted follow-ups: provider-wide delivery-state TTL/capacity backstop; separate `BUG-20260713-telegram-route-shapes` alternative-shape TechSpec; grandfathered over-cap owner splits before next touch; two small clarity/normalization nits.
- Completion rule: the SHIP verdict is provisional until the sequenced source-freeze gate passes. Any source change driven by runtime E2E, Web E2E, or `make verify` invalidates this verdict and requires round 3.

## Round 3

- Verdict: `SHIP`, subsequently invalidated by accepted risk R-001 because fixing it changes the reviewed source; a fresh consecutive round is mandatory.
- Findings source: `.peer-reviews/hermes-bridge-20260713T054134Z/impl-review-findings-round3.md`.
- Remediation scope: shared HTTP response cleanup with surfaced drain/close errors; fail-closed Linear signature handling; daemon-owned `webhook_public_url` projection and removal of the browser security-policy clone; one shared bridge provider-card composite; two factual docs corrections; structural extraction of bridge payload converters from the over-cap legacy conversions file.
- Current status: the full R-001 remediation is closed across all eight providers, strict ACK materialization, write-ahead/no-replay behavior, progress continuation, redirects, public send-test surfaces, docs, and QA. Round 3 remains invalidated because those fixes changed reviewed source.

## Round 4

- Status: explicitly waived by the user on 2026-07-13 after prior external rounds and multiple final internal audits; prepared prompt/diff/status artifacts were removed without launching `compozy exec`.
- Internal verdicts: code/runtime/docs/deslop are `SHIP`; the only readiness blocker was Daytona generated-asset drift and is resolved in the index.
- Codegen proof: the canonical full generator rebuilt both Daytona sidecars for the merged source, and `make codegen-check` proves they co-ship without drift.
- Completion rule: proceed directly to runtime E2E, Web E2E, and the single full `make verify`; any source change from those gates must still be remediated and reverified.
- Final-gate remediation: Runtime E2E exposed compile-time drift in the integration-tagged HTTP fixture (`CheckBridge` missing after `core.BridgeService` expanded). The HTTP and UDS fixtures now implement all three unconfigured control/delivery methods with canonical unavailable sentinels. Their canonical transport suites pass together under `-race` (22 tests); this source change requires a new checkpoint and a fresh restart of the three serial final gates, but the user-requested external-round waiver remains in force.
- Final-gate remediation continued after the clean Runtime E2E: Web E2E exposed five stale test contracts and a real repeated global model-source fetch. The browser fixtures now follow current daemon/CLI/route contracts and the provider-override scenario is offline and environment-hermetic. `CatalogService` takes one cloneable per-source snapshot for all provider projections; RED/GREEN call-count regressions and the full modelcatalog package (`-race`, 83.1% coverage) are green. These corrections do not reinstate the explicitly waived external review round; they require a fresh serial final-gate restart from the new checkpoint.
- The next Web E2E reached 65/70 and closed the remaining focused owners without reinstating review: Skills asserts the canonical installed detail route; provider override restores only the system `ps` path; session hard-delete now removes catalog plus dependent aggregates transactionally before artifacts; and permission decisions remain visible outside settled-turn folds. Manager/GlobalDB `-race` owners, rollback/retry cases, Web derivation/fixture tests, typecheck, scoped lint/vet, migration identity checks, CSV validation, and diff checks are green. Runtime E2E, Web E2E, and `make verify` must restart from the resulting checkpoint.
- Checkpoint `06e2f954b` then passed Runtime E2E and the full Web E2E with zero failed tests. Its first `make verify` stopped at the initial Oxfmt gate because 65 generated CLI parent pages were current but unformatted. Remediation is build/docs-only: `make cli-docs` now formats the full generated tree, subcommand-table rendering moved out of the 833-line legacy owner into a focused deterministic renderer, the exact owner test is red/green, full docpost `-race`/vet/lint pass, and two consecutive generations are byte-idempotent. The already-passing runtime/browser evidence remains valid; only `make verify` needs a fresh run on the new checkpoint.
- Subsequent clean gates closed two stale test contracts without changing runtime behavior: the Web registration fixture now supplies daemon-owned `webhook_public_url`, and the observe helper accepts the closed `DeliveryEventType`. Checkpoints `f01624152` and `e69bf2c3` carry those exact corrections; focused route/observe owners, typecheck, lint, vet, and race evidence are green.
- The next clean gate passed Oxfmt/Oxlint, all Turbo typechecks, all 367 Vitest files/3,258 tests, and the Web production build before global Go lint reported 28 source-freeze findings. Structural remediation removes every finding without suppression or public-contract change. The affected bridge-format, bridge-contract, CLI, codegen, extension-contract, and extension-test packages pass fresh combined `golangci-lint@v2.12.2`, `go vet`, and bounded `-race` suites. The only test correction aligns the older WhatsApp positive setup case with the already-canonical write-only secret rule; production remains unchanged. The explicit external-review waiver remains in force, and only the final clean `make verify` is pending.

## Test Placement

- B-001 invariant: the UDS golden registry exactly matches all registered public routes. Owning layer: UDS route integration. Canonical suite: `internal/api/udsapi/handlers_test.go`.
- B-002 invariant: every visible bridge metric is a direct daemon-owned datum with truthful semantics. Owning layer: Web component. Canonical suite: the existing bridge-detail metrics component suite/story; visual evidence through the existing bridge detail Storybook surface.
- B-003 invariant: a filter-less doctor run performs no live bridge provider checks; only explicit `--only bridge` opts in. Owning layer: doctor runner/core integration. Canonical suite: existing doctor category/filter tests after exact owner inspection.
- B-004 invariant: provider-side bridge packages cannot import extension, daemon, store, or session implementation graphs. Owning layer: static package boundary gate. Canonical suite: `magefiles/boundaries.go` and its existing tests.
- B-005 invariant: guided and JSON Telegram setup persist the operator-selected peer-only, ordinary-group, or forum route shape. Owning layer: CLI setup integration. Canonical suite: `internal/cli/bridge_setup_test.go` and existing setup integration owner where transport persistence must be proven.
- N-008 invariant: repeated progress events in the same phase update content without reapplying the phase reaction. Owning layer: bridgesdk accumulator/runtime unit. Canonical suite: `internal/bridgesdk/progress_test.go`.

## Decisions

- Remove fabricated `Events (24h)` and `Success rate` tiles; do not replace them with another derived estimate.
- Make one store-free bridge wire-contract package the canonical provider-facing owner, hard-cut bridgesdk/providers/subprocess/reference/generator roots to it, and enforce the complete dependency closure rather than only direct imports.
- Preserve the current Telegram forum selection as the default while making peer-only and ordinary-group shapes explicit inputs.
- Treat `BUG-20260712-reasoning-evidence-attribution` as part of final completion evidence: repair the diagnostics attribution owner before trusting the global runtime lane.
- Co-ship the Daytona sidecars produced by the canonical full codegen for the merged source; the final codegen check must prove their freshness.

## Progress

- Round 2 readiness initially stopped before external review: the aggregate `make codegen-check` was masked by the two unrelated modified Daytona sidecars, while the direct canonical generator check exposed stale `sdk/typescript/src/generated/contracts.ts` after the leaf-contract hard cut. `go run ./cmd/agh-codegen all` regenerated the artifact; canonical formatting reduced the delta to the six intended alias-placement/type-use lines. Direct Go→TS, OpenAPI→TypeScript, DESIGN sync, and formatting checks pass, and the root Turbo extension-SDK lane passes typecheck plus 49/49 tests with the already-proven non-Daytona codegen marker. The Daytona blobs remain untouched and excluded.
- Round 2 external review completed successfully from the repository parent after the installed Compozy 0.1.12 failed to decode the repo's newer workspace defaults before model launch. The retry used absolute contract/diff/findings paths, changed no source, passed the findings validator, preserved identical pre/post status, and returned SHIP.
- Round 1 findings and required skill references loaded.
- B-001..B-005 are remediated with focused red/green evidence: UDS route parity, truthful daemon-owned Web metrics, opt-in bridge doctor probes, selectable Telegram route shapes, and a canonical store-free provider wire leaf. B-004 closes direct and transitive dependency paths from bridgesdk, subprocess, all eight providers, and the Telegram reference into daemon bridge/store/resource/task implementations.
- N-001..N-008 are remediated. The new regressions prove Slack UTF-16 chunk limits, fractional Discord retry delays, and one reaction per phase transition; metadata errors are explicit, spec/Web type drift is removed, and the real CLI verbs are documented.
- The Hermes-comparison docs pass materially expanded `operations.mdx` and `setup-telegram.mdx`: secure lifecycle checkpoints, binding deletion, raw delivery-default replacement, bounded target discovery, typed Telegram JSON shapes, and per-shape proof. Docs conformance and site/Web typechecks are green.
- R-001..R-017 are locally remediated or proven already closed: R-008 was already covered by the DNS-resolving, IP-pinned, redirect-free webhook client; R-016 now has prefix-aware direct rules plus a fail-closed `go list -deps` closure gate around every provider-side root; R-017 is implemented under its separately approved harness TechSpec and ADR.
- B-004/R-016 focused closure evidence: the leaf contract suite passes under `-race` at 96.3%; every newly exported normalization/lifecycle wrapper is directly covered; canonical parent test owners were removed or narrowed; exact subprocess, extension contract, bridgesdk, provider/reference compile, SDK generation, and Mage boundary lanes pass; `make boundaries` is green; and structural extractions prevent positive growth in changed production files already over 500 lines.
- R-003/R-004 now use one shared `DeliveryChunkCursor` plus provider-local state retention and per-operation `RetryDo`. Slack, Telegram, Discord, Teams, Google Chat, and WhatsApp exhaust typed `Retry-After` attempts on the failing chunk, retain only the confirmed prefix, and resume at the first unsent chunk. All six complete provider packages and the complete bridgesdk package pass under `-race` (396 provider tests + 266 bridgesdk tests).
- R-017 reuses the existing correlation owner: every ACP subprocess already receives `AGH_SESSION_ID`. The central acpmock writer now stamps that owner and overwrites caller input; exact fail-closed selection occurs before protocol/prompt projection. A concurrent daemon regression launches two sessions through one fixture registration and shared JSONL, proving distinct AGH owners, equal non-empty process-local ACP IDs, and one ordered `model → effort → prompt` sequence per owner. Writer/subprocess/unit/E2E race tests, ten stress runs, scoped vet, marker/convention/diff checks, and independent review pass without production launch/API/config changes.
- Round 1 is ready for re-review. Global runtime/Web E2E and the single `make verify` remain deferred until all peer-review remediation reaches final source freeze.
- Targeted CLI-reference regeneration now reflects the secure secret-input hard cut: generated `bridge secret-bindings put` exposes `--secret-value-stdin` and `--secret-value-file`, not inline `--secret-value`; generated page metadata was normalized and passes `git diff --check`.
- The reopened Hermes documentation pass now tells the full executable lifecycle: source build/install because provider binaries are not shipped in release archives, disabled-provider catalog semantics, current Teams packaging, truthful Slack command/ACK/thread behavior, bounded SSE monitoring, concrete reverse-proxy/service deployment, exact probing, and desired-state decommission.
- Runtime-truth review fixed impossible or misleading route examples. Google Chat uses separate `group+thread` space and `peer+thread` DM shapes; Discord exposes the real group-only wizard default, its thread-collapse consequence, and separate thread-preserving/direct-DM policies. Telegram, WhatsApp, GitHub, and Linear passed mapper comparison.
- Public extension docs now mark `@agh/extension-sdk` as a repository-local workspace package and bridge adapters as trusted in-tree/local-source work; they no longer imply a complete published SDK/marketplace bridge workflow.
- Fresh focused evidence after these corrections: 23-file Oxfmt, exact bridge docs conformance under `-race`, and root Turbo site typecheck/MDX generation all pass. Global targets remain deferred to final source freeze.
- Final Hermes-parity gaps are closed: every directly navigable provider guide now proves provider installation/catalog health; the Slack overview uses the safe group-only `/agh` shape; all provider READMEs lead to their operator guide and show complete callback configuration; and `operations.mdx` documents a real local managed-provider upgrade/rollback with prebuilt known-good artifact, exact instance inventory, shared-process containment, canary proofs, and external-contract reversal. Read-only runtime review confirmed remove/install, catalog pagination, provenance, auto-enable, and bridge-resource persistence; unsupported Discord-intent and Teams-command-list wording was removed.
- The final independent Hermes-parity audit returned `PASS` with no residual findings and confirmed every new relative README link resolves. No global gate ran; focused docs conformance, site MDX/typecheck, Oxfmt, and diff checks are the current evidence.
- Phase D QA impact is reconciled without rewriting history: `NB-bridge-provider-setup` is reset from `blocked-decision` to `untested` after guided and strict-JSON Telegram setup gained explicit private/group/forum shapes. `BUG-20260713-telegram-route-shapes` remains attached to the broader one-instance alternative-shape contract; the exact `TestBridgeSetupTelegramRoutingShapes` owner passes under `-race`, and the living scenario tree materializes without duplicate IDs.
- Complete QA impact reconciliation now resets `NB-037`, `NB-bridge-tool-progress`, `NB-long-bridge-replies`, `NB-provider-progress-rendering`, `NB-bridge-provider-setup`, `NB-web-bridge-setup`, and `NB-bridge-restart-recovery` to `untested`, retains all historical evidence, and records no QA rerun. `NB-web-bridge-setup` was reset only after the separate 320px setup reflow changed its secret/config surface. Slack progress and terminal delivery now consistently use 40,000 UTF-16 code units.
- B-002's required Storybook capture passes for the six truthful metrics. The same 320px surface exposed and closed a separate 36px overflow below them: scroller width is now `318/318`, metadata/Fields/cards have zero overflow, desktop remains unchanged, same-story captures were inspected, focused Web gates pass, and teardown is clean.
- The deterministic projection test failure was a stale R-006 assertion, not a production regression: canonical `ToolInput` stays redacted while heuristic terminal chrome is intentionally command-name-only. Existing extension/bridges owners now assert exact safe previews and absence of arguments/secrets; focused race lanes pass.
- The final post-review residue pass is closed. Daemon-owned `webhook_public_url` replaces the browser security-policy clone; bridge cards share one domain component; the create dialog reflows at 320px; docs import the leaf contract and match the real secret/Discord requirements; bridge payload conversion left the over-cap legacy file; response cleanup cannot retry a committed create/update/delete/affordance; Linear HMAC fails closed; `RouteTable.Replace` and GitHub test teardown no longer discard errors. Independent docs and API/Web audits returned SHIP, the focused Go owner passed 23 tests across five packages under `-race`, Web passed 51/51, site truth passed 24/24, docs conformance passed 4/4, and direct codegen check is clean.
- Source freeze v3 closes the strict wire/documentation edge cases: explicit matching `delivery_id` and integer `seq` are required, explicit `seq: 0` is valid, missing/null/wrong-type/mismatched ACKs conservatively become `committed_result_unavailable`, and restart reconciliation distinguishes unmatched intent from checkpoint-only fail-open. `NB-bridge-restart-recovery`, `NB-indeterminate-bridge-delivery`, and the author/operations/routing docs match the final runtime.
- Fresh focused evidence is green: bridge/extension contract owners under `-race`, eight providers under `-race`, API/CLI send-test owners, Web dialog 6/6, site truth 14/14, provider docs conformance 4/4, scoped Go lint, Oxfmt/Oxlint, CSV/nav validation, and diff-check. Deslop confirmed every new production Go file below 500 lines and no existing over-cap production file grew.
